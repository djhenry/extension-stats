FROM scratch
LABEL org.opencontainers.image.title="Container Stats" \
      org.opencontainers.image.description="Real-time container and host resource monitoring dashboard" \
      org.opencontainers.image.vendor="podman-desktop" \
      io.podman-desktop.api.version=">= 1.0.0"

COPY package.json /extension/
COPY dist /extension/dist
COPY media /extension/media
COPY icon.png /extension/
